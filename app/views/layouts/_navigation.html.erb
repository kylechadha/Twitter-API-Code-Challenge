<nav class="top-bar" data-topbar>
  <ul class="title-area">
    <li class="name">
      <h1><a href="#">BuzzFeed Tweet Portal</a></h1>
    </li>
     <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
      <% if current_user %>
        <li><%= link_to "#{current_user.name}", logout_path %></li>
        <li class="active"><%= link_to "Sign Out", logout_path %></li>
      <% else %>
        <li class="active"><%= link_to "Sign in with Twitter!", "/auth/twitter" %></li>
      <% end %>
    </ul>

  </section>
</nav>